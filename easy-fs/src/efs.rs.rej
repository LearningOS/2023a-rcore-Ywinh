diff a/easy-fs/src/efs.rs b/easy-fs/src/efs.rs	(rejected hunks)
@@ -111,6 +111,15 @@ impl EasyFileSystem {
         // release efs lock
         Inode::new(block_id, block_offset, Arc::clone(efs), block_device)
     }
+
+    ///其实就是下面get_disk_inode_pos的逆运算，注意offset是字节数
+    pub fn get_inode_id(&self, block_id: usize, offset: usize) -> u32 {
+        let inode_size = core::mem::size_of::<DiskInode>();
+        let inodes_per_block = (BLOCK_SZ / inode_size) as u32;
+        (block_id as u32 - self.inode_area_start_block) * inodes_per_block
+            + (offset / inode_size) as u32
+    }
+
     /// Get inode by id
     pub fn get_disk_inode_pos(&self, inode_id: u32) -> (u32, usize) {
         let inode_size = core::mem::size_of::<DiskInode>();
